'use strict';

/**
 * inbound-receiving-form controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::inbound-receiving-form.inbound-receiving-form');
